---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-16.04
#  - name: centos-7.1

suites:
  - name: default
    run_list:
      - recipe[n1nj4-users::default]
    attributes:
